1. Make sure to have `gcc` and `make` installed for `UCRT64`
2. Use the `make <command from makefile>` to build or clean the project

`make all` - builds entire project
`make clean` - cleans up build folder